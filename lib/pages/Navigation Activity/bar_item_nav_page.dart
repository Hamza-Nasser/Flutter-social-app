import 'package:flutter/material.dart';

class BarItemNavPage extends StatelessWidget {
  const BarItemNavPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Bar Item Page'
      ),
    );
  }
}
